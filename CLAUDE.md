# CLAUDE.md
## 0. 项目名称

一一丫丫学拼音

## 1. 项目定位

本项目是一个面向 **6-7 岁儿童** 的 AI 拼音学习系统，核心目标不是“播放课件”，而是让 AI 按孩子真实掌握情况，完成：

1. 首次建档
2. 拼音能力测评
3. 个性化学习计划生成
4. 单拼音教学 + 发音纠错
5. 画板联想创作 + 图生图
6. 次日复习与薄弱点强化

产品核心闭环：

```text
登录 → 建档 → 测评/定级 → 制定计划 → 教学 → 跟读评测 → 画画创作 → 结束/休息 → 次日复习
```
特别注意！！！！孩子是很讨厌等待的，如果在开发过程中，某个环节很耗时，你要想一下有没有什么办法减少耗时，让孩子的体验更加顺滑。
---

## 2. 项目目标

### 2.1 业务目标

- 让孩子以“对话 + 跟读 + 绘画”的方式学习拼音
- AI 根据孩子的年龄、姓名、是否学过拼音、当前进度、历史薄弱点动态调整教学
- 所有学习流程必须围绕“孩子是否真的会读、会认、会复习”设计，而不是固定播放脚本

### 2.2 产品原则

- **以孩子体验为中心**：反馈必须鼓励式、短句、可理解
- **以掌握度为中心**：是否进入下一个拼音，由测评结果决定
- **以复习效果为中心**：按艾宾浩斯记忆曲线安排复习
- **以服务端状态为中心**：学习进度、时长、计划、薄弱点全部以后端为准

---

## 3. 当前范围与非范围

### 3.1 当前范围

- 学生端登录后完整学习闭环
- 首次用户信息采集
- 拼音基础测评
- 个性化学习计划生成
- 单拼音教学、跟读评测、错误反馈
- 画板创作、图生图、作品回看
- 每日 15 分钟学习限制
- 次日复习与薄弱点强化

### 3.2 不在当前范围

- 家长端
- 管理后台
- 商业化支付
- 多角色权限体系
- 大规模运维与 CI/CD 体系

---

## 4. 目标用户与核心信息

### 4.1 目标用户

- 4-8 岁儿童
- 首次接触拼音或已有部分基础

### 4.2 首次必须采集并落库的信息

- 孩子姓名
- 孩子年龄
- 是否学过拼音

这三个字段是首轮对话的硬要求，不能只存在上下文里，必须落库。

---

## 5. 核心业务流程

### 5.1 登录

- 用户进入后先登录
- 首次阶段允许测试账号，例如：`username_11 / 123456`

### 5.2 首次建档

- AI 主动收集姓名、年龄、是否学过拼音
- 收集完成后创建儿童档案

### 5.3 拼音测评

- 如果从未学过拼音：默认从 `a` 开始
- 如果学过拼音：从 `a、o、e...` 顺序开始测评
- 每个拼音展示卡片，由 LLM 引导孩子朗读
- 孩子朗读时调用 **讯飞语音测评能力**
- LLM 根据测评结果给反馈，并判断是否继续向后测
- 直到识别出孩子不会的位置，作为后续学习起点

### 5.4 学习计划

- 测评完成后生成个性化学习计划
- 计划需结合 **艾宾浩斯记忆曲线**
- 计划输出必须可落库、可执行、可追踪

### 5.5 单拼音教学

- 教授拼音本体怎么读
- 教授不同音调
- 教授发音技巧
- 教学文案可预生成，运行时由 LLM 按需调用

### 5.6 跟读练习与纠错

- 教完一个拼音后，要给出相关汉字/组合音进行练习
- 孩子需要读出来
- 调用讯飞评测判断读音是否正确
- 正确：给鼓励
- 错误：明确指出哪里读得不好
- 单次错误最多额外练 3 次
- 超过 3 次仍不理想：记录为薄弱点，加入后续重点复习

### 5.7 绘画与图生图

- 每学完一个拼音，给孩子一个画板
- AI 用语音引导孩子把这个拼音联想到的内容画出来
- 画完后调用豆包图生图模型生成涂鸦照片
- 生成后 AI 继续围绕作品和孩子聊天

### 5.8 学习完成与继续判断

- 单个拼音的画作完成后，视为该拼音完成一次完整学习
- AI 需要询问孩子学习感受
- 根据孩子反馈与当日时长，判断继续还是休息

### 5.9 每日时长限制

- 单日学习总时长 **不要超过 20 分钟**
- 但以“当前拼音是否完整学完”为边界
- 如果一个拼音完整结束后，当日累计已超 20 分钟，则当天必须停止，不能再开新拼音

### 5.10 次日复习

- 下一次学习时，先展示孩子自己的画作
- 引导孩子回忆这是什么拼音、读出来
- 再结合上次记录的薄弱点做重点复习

---

## 6. 系统角色划分

### 6.1 LLM 的职责

- 控制对话节奏
- 控制测评流程
- 控制教学流程
- 根据测评结果生成反馈
- 根据孩子状态决定继续、复习、休息或结束

### 6.2 工具能力的职责

- **讯飞语音测评**：负责发音评测，本身不是教学流程控制器
- **豆包图生图**：负责将孩子画作生成视觉成品
- **TTS / ASR / LLM 编排层**：负责语音输入输出与对话执行

原则：**工具负责能力，LLM 负责流程。**

---

## 7. 核心数据要求

至少应有以下数据对象：

- `children`：儿童档案
- `assessments`：首次测评过程与定级结果
- `learning_plans`：学习计划与学习频率
- `pinyin_progress`：当前学习进度
- `pronunciation_records`：每次跟读评测结果
- `weak_points`：错误音、薄弱拼音、失败次数
- `drawings`：孩子原始画作与生成图片
- `session_records`：每日学习时长与完成状态

硬性要求：

- 首次采集信息必须持久化
- 测评结果必须可回放、可解释
- 薄弱点必须可用于次日复习
- 学习时长必须服务端可计算，不允许只靠前端计时

---

## 8. 目录与文件规范

当前仓库已有较多历史文件。自本说明生效后，目录规则统一如下：

### 8.1 主目录职责

- `frontend/`：学生端前端应用
- `backend/`：后端服务、Agent、数据库、业务编排
- `backend/services/`：外部能力封装（讯飞、豆包、TTS、ASR 等）
- `backend/agents/`：欢迎、测评、教学、绘画、复习等 Agent
- `backend/sql/`：DDL、迁移草案、初始化 SQL

### 8.2 根目录允许内容

根目录只允许放：

- 项目级说明文档（如 `CLAUDE.md`、`DEV_SPEC.md`、`ACCEPTANCE_TEST_CASES.md`）
- 顶层配置文件
- 明确标注为历史参考的资料

### 8.3 新增文件规则

- 新前端代码只放到 `frontend/src/`
- 新后端代码只放到 `backend/`
- 新文档优先放到已有文档体系，避免继续把零散实现文件堆在根目录
- 临时调试脚本不得长期留在根目录

### 8.4 遗留资产处理原则

- 根目录已有 `proxy.py`、`orchestrator.py`、协议文件、接入文档等，可作为参考资产
- 如功能已迁入 `backend/`，后续开发优先以 `backend/` 为主线，不再扩散新的平行实现

---

## 9. 技术实现约束

### 9.1 架构约束

- 前端主要负责交互展示、录音、画板、播放
- 后端负责状态机、Agent 调度、学习进度、时长控制、计划生成
- 关键业务状态以后端为准

### 9.2 语音评测约束

讯飞测评接入必须遵守现有接入文档中的限制，包括但不限于：

- WebSocket 接口地址固定
- HMAC-SHA256 鉴权
- `date` 使用 GMT / UTC
- 两帧发送约束
- 音频格式为 `lame MP3, 16kHz, 16bit, 单声道`
- `refPinyin` 使用数字声调，如 `ni3 hao3`

### 9.3 学习流程约束

- 不允许跳过首次建档直接开始正式学习
- 不允许把“学完一个拼音”的定义收缩为仅朗读成功
- 必须包含教学、练习、反馈、绘画/联想四个环节后，才算完成一个拼音
- 不允许忽略错误记录；错误必须沉淀到薄弱点

---

## 10. 开发原则

### 10.1 先规则后开发

- 新功能先明确放在哪个目录、归哪个模块
- 没有归属的代码不写
- 先改文档与约束，再改实现

### 10.2 小步变更

- 优先做小而清晰的改动
- 禁止在 bugfix 中顺手做大重构
- 禁止复制出第二套相同职责的实现

### 10.3 数据优先于感觉

- 是否学会，靠评测结果和历史记录判断
- 是否该复习，靠计划和薄弱点判断
- 是否该结束当天学习，靠服务端累计时长判断

### 10.4 儿童交互原则

- 文案短、正向、清晰
- 纠错不能只有“错了”，必须指出哪里不对
- 鼓励要具体，不要模板化堆砌

---

## 11. 开发验收基线

任何新功能至少满足：

1. 能映射到明确业务环节
2. 能说明数据落在哪里
3. 能说明失败/异常时如何处理
4. 不破坏每日 20 分钟限制
5. 不绕过测评、薄弱点记录、复习链路

如果某项实现无法回答“孩子学到了什么、系统如何知道、下次如何接着教”，说明设计还不完整。

---

## 12. 默认判断规则

后续开发默认按以下判断执行：

- 首次用户必须先建档
- 有基础用户先测评再开始正式教学
- 单拼音完成 = 教学完成 + 跟读评测 + 错误处理 + 绘画完成
- 薄弱点自动进入后续复习优先级
- 超过 20 分钟后，当天不再开启新拼音

---

## 13. 一句话总结

这不是一个“拼音内容展示项目”，而是一个 **由 LLM 统筹、语音评测驱动、绘画增强记忆、可持续复习的儿童拼音学习系统**。


## 14. 注意事项！！！
## 用户体验
特别注意！！！！孩子是很讨厌等待的，如果在开发过程中，某个环节很耗时，你要想一下有没有什么办法减少耗时，让孩子的体验更加顺滑。
所以，在使用ASR、TTS的时候，以及生成图片的时候，要注意这些细节。特别是在生成图片的时候，要特别特别注意，因为服务商没有办法那么快返回生成好的图片，所以你要有些过度的有趣的loading样式！！！

## 在LLM的prompt中使用

在所有发送给 TTS 的文本，如果包含拼音字母，必须使用以下方法附加参照汉字，因为不然TTS模型会用英语发音！！切记切记！！

```python
# 在 Agent 的 process() 中
Pinyin_REF_CHAR = {
    "b": "波", "p": "泼", "m": "摸", "f": "佛", "d": "嘚", "t": "特",
    "n": "讷", "l": "嘞", "g": "哥", "k": "科", "h": "喝",
    "j": "鸡", "q": "七", "x": "西",
    "zh": "知", "ch": "吃", "sh": "诗", "r": "日",
    "z": "资", "c": "呲", "s": "思", "y": "衣", "w": "乌",
    "a": "啊", "o": "喔", "e": "婀", "i": "衣", "u": "乌", "ü": "迂",
    "ai": "爱", "ei": "诶", "ui": "威", "ao": "奥", "ou": "欧", "iu": "优",
    "ie": "耶", "üe": "约", "er": "儿",
    "an": "安", "en": "恩", "in": "因", "un": "温", "ün": "晕",
    "ang": "肮", "eng": "鞥", "ing": "英", "ong": "轰",
    "zhi": "知", "chi": "吃", "shi": "诗", "ri": "日",
    "zi": "资", "ci": "呲", "si": "思",
    "yi": "衣", "wu": "乌", "yu": "迂", "ye": "耶", "yue": "约",
    "yuan": "冤", "yin": "因", "yun": "晕", "ying": "英",
}

